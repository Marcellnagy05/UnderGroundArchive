<Window x:Class="UnderGroundArchive_WPF.Views.RequestView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UnderGroundArchive_WPF.Views"
        mc:Ignorable="d"
        Title="RequestView" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0">
            <Button Content="Load Pending Requests" Command="{Binding LoadRequestsCommand}" Margin="5" />
            <Button Content="Accept Request" Command="{Binding AcceptRequestCommand}" Margin="5" />
            <Button Content="Deny Request" Command="{Binding DenyRequestCommand}" Margin="5" />
        </StackPanel>
        <Button Content="Go Back" Command="{Binding GoToUsersCommand}" Margin="5" Width="150" HorizontalAlignment="Right"/>
        <DataGrid ItemsSource="{Binding Requests}" SelectedItem="{Binding SelectedRequest}" Grid.Row="1" AutoGenerateColumns="True" />
    </Grid>
</Window>
